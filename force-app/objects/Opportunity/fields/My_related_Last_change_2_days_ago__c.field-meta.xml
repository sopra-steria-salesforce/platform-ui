<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>My_related_Last_change_2_days_ago__c</fullName>
    <formula>IF( (( 
$User.Id = Account.OwnerId || 
$User.Id = OwnerId || 
$User.Id = Apps_Owner__r.Id || 
$User.Id = BC_Owner__r.Id || 
$User.Id = DPS_Owner__r.Id || 
$User.Id = BID_Office_Owner__r.Id)&amp;&amp; 
LastModifiedDate &gt; DATETIMEVALUE( Today()-2 )  
), True , False )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>My related _Last change 2 days ago</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
