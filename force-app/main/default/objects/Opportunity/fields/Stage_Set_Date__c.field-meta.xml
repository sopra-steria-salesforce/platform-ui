<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Stage_Set_Date__c</fullName>
    <description>Shows Date when current StageName was set, including for new Oppties where stage has not been changed, opposite to standard field LastStageChangeDate</description>
    <externalId>false</externalId>
    <formula>If(ISNULL( LastStageChangeDate) , DATEVALUE(CreatedDate), DATEVALUE(LastStageChangeDate))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Stage Set Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
