<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_to_Me__c</fullName>
    <description>True if current user is Account owner, Owner, BU owner or BID Office Owner to the opportunity</description>
    <externalId>false</externalId>
    <formula>IF( (
$User.Id = Account.OwnerId ||
$User.Id = OwnerId ||
$User.Id = Apps_Owner__r.Id ||
$User.Id = BC_Owner__r.Id ||
$User.Id = DPS_Owner__r.Id ||
$User.Id = BID_Office_Owner__r.Id), True , False )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Related to Me</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
