<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Owner_Blocked_Account__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Used to maintain owner for Blocked account. Account will always be visible for Account owner, so we need to replace owner when account = blocked</description>
    <externalId>false</externalId>
    <label>Owner Blocked Account</label>
    <referenceTo>User</referenceTo>
    <relationshipName>Accounts6</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <type>Lookup</type>
</CustomField>
